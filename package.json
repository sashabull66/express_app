{
  "name": "express",
  "version": "1.0.0",
  "main": "src/main.ts",
  "type": "module",
  "author": "sasnabull",
  "license": "UNLICENSED",
  "private": false,
  "scripts": {
    "build:clean": "rm -rf dist",
    "build": "yarn build:clean && tsc",
    "start": "yarn build && node dist/main.js || yarn build:clean"
  },
  "devDependencies": {
    "@types/express": "^4.17.17"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "inversify": "^6.0.1",
    "reflect-metadata": "^0.1.13",
    "tslog": "^4.8.2"
  }
}
